


echo "resource/ui/spectator.res"
echo "{"

// Spectator
echo "Spectator { visible 0 enabled 0 }"

// Remove o background no modo Spectator para Windows (incluindo o cl_hud_minmode 1) e X360
echo "topbar{ tall 0 tall_minmode 0 tall 0}"

// Parece não haver outra alternativa para a remoção do background cinza presente no modo Spectator
echo "bottombarblank { xpos 99999 ypos 99999 } "



// Para mim, apenas a remoção daquele background gigantesco é suficiente para atingir um estado minimalista aceitável
// Portanto, as opções abaixo, por serem informações cruciais, não deveriam ser excluídas totalmente.

// Map name

echo "maplabel { visible 0 enabled 0}"
echo "ClassOrTeamLabel {visible 0 enabled 0} "

// Informa qual Key é usado para alternar o modo 'Camera'. Tais opções devem ser ativadas ou desativadas em conjunto
echo "SwitchCamModeKeyLabel{ xpos 99999 visible 0 enabled 0 }"
echo "SwitchCamModeLabel { xpos 99999 visible 0 enabled 0}"

// Avança na lista de espectatores
echo "CycleTargetFwdKeyLabel{ xpos 99999}"
echo "CycleTargetFwdLabel{ xpos 99999}"

// Retrocede na lista de espectadores

echo "CycleTargetRevKeyLabel{ xpos 99999}"
echo "CycleTargetRevLabel{ xpos 99999}"


// Ter um bloco de texto contendo dicas não parece ser muito útil no modo Spectator. 
echo "tiplabel {xpos 99999}"





echo "}"


